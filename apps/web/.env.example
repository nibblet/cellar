# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://jafcwggqgqxrcbuxjreo.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_FulYj4sYNpyiOf_wVCykNw_OdSK2Id3

# Service role — server only, NEVER commit, NEVER expose to client
# Get from Supabase dashboard → Project Settings → API → service_role
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI — server only
# Models in use: gpt-5-mini (vision + pairing prose), gpt-5-nano (chip→wheel mapper)
OPENAI_API_KEY=

# Replicate — for CLIP image embeddings (Phase 2)
REPLICATE_API_TOKEN=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# MCP — server only; shared club token for Claude connectors (generate with: openssl rand -hex 32)
NCCC_MCP_TOKEN=
