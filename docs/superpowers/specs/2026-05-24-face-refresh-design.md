# WS5+WS2 — Face Refresh Design

**Status:** Approved implicitly (prior brainstorm 2026-05-24). Build in progress.
**Prior workstream:** WS4 You hub (shipped).

## Scope

### Product detail v3
- Winston-narrated **THE CLUB SAYS** intro (italic Playfair via `<Voice />`)
- **Segmented brass control** for Edit tasting / Open a Session (equal weight, one component)
- **Inline depth** — Construction, Facts, Flavor profile on the main scroll; remove DepthAffordance tap-through
- **Pairs With** — ensure section surfaces candidates (lower display threshold on face if needed)
- **Catalog image correction** — "Wrong photo?" → member upload replaces `products.image_url` (soft-revert; admin rollback later)

### Catalog polish
- Larger Have/Tried/Want icons on feed cards + `title` tooltips (mobile popover follow-up)
- Filters: **brand/manufacturer**, **vitola**, **size** (ring gauge band)

## Out of scope
- Discover questionnaire (WS1)
- Capture-a-pairing (WS3)
- Tap-popover badges on member roster (follow-up ticket)
